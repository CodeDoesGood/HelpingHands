<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_first_page"
    android:alpha="0.8"
    tools:context="helpinghands.codedoesgood.org.helpinghands.FirstPageActivity">

    <TextView
        android:id="@+id/title_first_page"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="@string/app_name"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textAlignment="center" />

    <ImageView
        android:id="@+id/logo_first_page"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_below="@id/title_first_page"
        android:layout_marginTop="40dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_helpinghands_white_96dp"/>

    <TextView
        android:id="@+id/description_first_page"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/logo_first_page"
        android:padding="20dp"
        android:textSize="12sp"
        android:textColor="@android:color/black"
        android:text="@string/first_page_description"
        android:textAlignment="center"
        />

    <Button
        android:id="@+id/continue_with_sign_in_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="10dp"
        android:textAllCaps="false"
        android:text="@string/selector_button_string_continue_sign_in"
        android:textColor="@android:color/white"/>

    <Button
        android:id="@+id/quick_tour_button"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:background="@drawable/selector_button_quick_tour"
        android:layout_above="@id/continue_with_sign_in"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:layout_marginBottom="10dp"
        android:textAllCaps="false"
        android:text="@string/selector_button_string_quick_tour"
        android:textColor="@android:color/white"/>



</RelativeLayout>
